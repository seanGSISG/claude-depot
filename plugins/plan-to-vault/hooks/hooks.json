{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"if(!process.env.CLAUDE_PLUGIN_OPTION_VAULT_PATH){process.stdout.write(JSON.stringify({additionalContext:'[plan-to-vault] Vault path not configured. Run /plugin → plan-to-vault → Configure options to set your Obsidian vault path.'}))}\"",
            "timeout": 3
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "ExitPlanMode",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/copy-plan-to-vault.js\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
